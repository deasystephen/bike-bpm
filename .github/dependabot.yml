version: 2
updates:
  # The app itself has no package dependencies; the deploy workflow's
  # GitHub Actions are the only thing with versions to keep current.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
